{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "noetive": {
      "args": [
        "-y",
        "@noetive/mcp-server"
      ],
      "command": "npx",
      "env": {
        "NOETIVE_KEY_SECRET": "${NOETIVE_KEY_SECRET}"
      }
    }
  }
}
