{
  "mcpServers": {
    "noetive-mcp": {
      "args": [
        "-y",
        "@noetive/mcp-server"
      ],
      "command": "npx",
      "env": {
        "NOETIVE_KEY_SECRET": "${NOETIVE_KEY_SECRET}"
      }
    }
  }
}
